<p id="notice"><%= notice %></p>

<h1>Mdns</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Service</th>
      <th>Protocol</th>
      <th>Device</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mdns.each do |mdn| %>
      <tr>
        <td><%= mdn.name %></td>
        <td><%= mdn.service %></td>
        <td><%= mdn.protocol %></td>
        <td><%= mdn.device %></td>
        <td><%= link_to 'Show', mdn %></td>
        <td><%= link_to 'Edit', edit_mdn_path(mdn) %></td>
        <td><%= link_to 'Destroy', mdn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mdn', new_mdn_path %>
