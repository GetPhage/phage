<%= form_with(model: geo_location, local: true) do |form| %>
  <% if geo_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geo_location.errors.count, "error") %> prohibited this geo_location from being saved:</h2>

      <ul>
      <% geo_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :geoname_id %>
    <%= form.number_field :geoname_id, id: :geo_location_geoname_id %>
  </div>

  <div class="field">
    <%= form.label :continent %>
    <%= form.text_field :continent, id: :geo_location_continent %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country, id: :geo_location_country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
